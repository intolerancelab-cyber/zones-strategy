# DASH_BUILD_PASS - 5 Sep 2026 (London)

David full Prompt Dash demo cockpit pass (executor).
Paths: /workspace/prompt-dash-web (UI SoT), /workspace/prompt-dash (paper), sync to /workspace/zones-strategy-push.
No git push. No live API/workers. No invented POI/banding lists. No Tick ideas panel (David tests elsewhere).

---

## Audit vs paper locks (before / after)

| Lock / surface | Status in UI |
|----------------|--------------|
| DAVID_GRILL_LOCKS (KEEP PF>1.2; sweep >=0.75; Q4; Q5; Q6 FOR NOW) | Added grill locks summary strip + STRATEGY INPUT note |
| HANDOFF stages 1-21 + dual-lamp (10-11 then 12-13) | Solid - stages map + dual-lamp note in footer |
| Walk-forward (Q5) | Added lamp/stamp demo on stages 11-13; weak blocks Pass CHECK |
| Q6 single-recipe FOR NOW | Present in stage notes / talk-box label / grill strip |
| Take geometry (Q4) | Present in grill strip + STRATEGY INPUT note |
| DEMO honesty banners | Solid - splash, header chip, fixed bar (compact on mobile) |
| CHECK assault + talk boxes | Solid - 4-agent assault; talk boxes 3 / 9 / 12 |
| PARKED placeholders | Updated to four-pack (below) - no invented lists |

---

## Added this pass

1. Grill locks summary strip under STRATEGY INPUT.
2. PARKED four-pack cards (other session fills together, not now):
   - Full baseline recipe
   - Full POI sweep recipe
   - Full banding structure
   - POI list (PARKED - DANGEROUSLY SILENT)
3. On/off filters + holy-grail table numbers also parked; tick ideas tested elsewhere - not on this dash.
4. Walk-forward lamp/stamp on stages 11-13 (demo). Weak WF blocks Pass CHECK.
5. Mobile demo-bar compact.
6. Operator docs pointer + README link to docs/MONDAY_OPERATOR_RUNBOOK.md.
7. Store bump prompt-dash-web-v3 (walkForward; migrates from v2/v1).
8. Production build passed (Next.js 15.5.2).
9. Synced UI + docs into zones-strategy-push (no push).

---

## Still waiting on other session

Drop together (do not invent here):

1. Full baseline recipe
2. Full POI sweep recipe
3. Full banding structure
4. POI list (cleaned keep/cut)

Also later / parked: on/off filter list detail, final table numbers, Continuation entry-method lock, overnight HB / auth / emit / factory wiring.

Tick ideas Tier A-D remain paper-only (TICK_DATA_IDEAS_2026-09-05.md) - not a dash panel (David lock).

---

## Explicit non-goals this pass

- No Tick ideas UI
- No invented POI / banding / table / recipe lists
- No live API or workers
- No git push

---

## How to verify

From /workspace/prompt-dash-web run production build (must pass). Optional local start on port 3000.
