> **ORB DEAD — must not appear in this stage or any later prompt.**

# Stage 1 — Freeze / admit WORK handbook (5 Sep 2026)

Owner: DASHBOARD OVERSEER. Paper only. No factory code edits.
Source locks: CURRENT_STAGE_MAP.md, PROMPT_DASH_HANDOFF.md, WORK_HANDBOOK_TEMPLATE.md.

**Product dead (never revive):** Doubles/pairs = DEFUNCT. ORB = DEAD.
**Fleet:** ES + NQ × 5m + 15m only (no 1m). 10% progress strips per cell; orange CHECK only when all green.
**Fork:** hooks only.
**Band PF provisional:** skip <0.75; revival ~0.75; boost lines 1.0|1.2 (not locked).
**On/off filters:** binary no-line states; parked — ask David later.

---

## MUST-SHARD vs MUST-NOT (this stage)

| MUST-SHARD (heavy compute) | MUST-NOT (identity / decision) |
|----------------------------|--------------------------------|
| *(none for Freeze itself)* | Freezes / admit seals |
| Bar load *(later stages)* | Checks (all of them) |
| POI map / Baseline / Band / Boost / Assemble / Stress / MC *(later)* | Take / KEEP decisions |
| | Hold / grade alone |
| | All-cell barrier wait (100% fleet) |
| | Speedy lamp / emit-covered report steps |
| | Champion talk-box choice |
| | STRATEGY INPUT binding once sealed |
| | Cost / session lock values once stamped |

**Hard rule for Stage 1:** Freeze/admit is **MUST-NOT heavy**. Do **not** shard the freeze itself. Identity seals run one-core, sequential, book-level. Wait 100% across all fleet cells before any later CHECK unlock. Do not shard the barrier wait itself.

**What gets locked here for later stages (read by every downstream WORK/CHECK):**
- Cost model (present before any PF talk)
- Session calendar / RTH vs overnight filter locks
- Fleet roster: ES+NQ × 5m+15m only (1m off)
- ORB DEAD + doubles DEFUNCT attestation
- STRATEGY INPUT checkbox binding (Reversal LOCKED 6 vs Continuation OPEN minus orb_*)
- Fork = hooks only
- Band PF provisional lines disclosed as provisional (not invented locks)
- Paper-room scope (no live / fold / VPS from this room)

---

### 0. Header
- **Stage # / name:** 1 — Freeze / admit (+ cost/session locks; no ORB)
- **Kind:** WORK
- **Goal (one sentence):** Seal cost model, session locks, fleet roster, STRATEGY INPUT checkbox, and product-dead attestations so every later stage reads one identity book — no ORB, no doubles, no invented OPEN David locks.
- **Unlocks next:** Stage 2 Baseline alone WORK (Stage 3 CHECK comes after Baseline; Freeze itself has no orange CHECK gate — stamps feed Baseline)
- **Fleet cells:** ES 5m, ES 15m, NQ 5m, NQ 15m
- **STRATEGY INPUT:** Reversal (LOCKED 6) vs Continuation (OPEN, catalog minus orb_*)

---

### 1. Goal
- **Done means:**
  - Book-level freeze/admit stamps written with cost model, session locks, fleet roster, STRATEGY INPUT binding, ORB-DEAD + doubles-DEFUNCT attestations.
  - Per-cell admit strips painted 10%…100% green (identity verify per cell, not heavy compute).
  - Downstream-readable lock pack path stamped so Stage 2+ never invents cost/session/entry-set.
- **This stage intentionally does NOT decide:**
  - Baseline PF / champion (Stage 2–3)
  - POI list (OPEN with David)
  - Band PF 0.75/1.2 locks (provisional only — disclose, do not lock)
  - Continuation set final shortlist (OPEN)
  - Take rule / on/off filter list / champion bank choice
  - Any KEEP / take / hold / grade decision

---

### 2. Stamped inputs (must exist before start)
List prior-stage stamp paths / hashes. If missing → STOP, do not invent.
- **Freeze/admit:** *(this is Stage 1 — no prior freeze)* — require room briefing pins only:
  - `CURRENT_STAGE_MAP.md` + `PROMPT_DASH_HANDOFF.md` readable
  - STRATEGY INPUT checkbox present on dash (Reversal vs Continuation)
- **Prior WORK stamps:** none (first WORK)
- **Cache / hash pins available:** none required yet; do not invent bar/POI hashes
- **Entry-method set in force:** bind from current STRATEGY INPUT checkbox value at seal time
  - Reversal LOCKED: `close_back_inside`, `n_close_back_inside`, `wick_rejection_completed_bar`, `touch`, `close_confirm`, `pullback_to_level`
  - Continuation OPEN: catalog minus `orb_*` — do not claim locked; stamp as OPEN

---

### 3. Full sharding handbook
#### 3a. Split
- **Unit:** **MUST-NOT** — do not time-shard / POI-shard / recipe-shard the freeze.
- Admit verify may walk four fleet cells **sequentially** (identity checklist per cell): roster folder, TF in {5m,15m}, no 1m, no ORB/doubles residue in planned UI copy.
- **Shard key / item_count:** N/A for freeze core; cell admit checks = 4 items max → `workers_used = 1` (or min(requested, 4) if a trivial parallel verify is allowed — never stamp 64).
- **One-cell smoke first?** Optional identity smoke on one cell checklist before painting all four — YES for strip paint honesty; NOT a heavy compute smoke.

#### 3b. Cores
- Default `n_workers=1`; **do not opt-in high-n for Freeze**.
- Target machines: any approved paper/side-copy machine for stamp write; **EPYC 64 / Gaming max cores are for later MUST-SHARD stages (Baseline+), not here**.
- Honest `workers_used = min(requested, item_count)` — Freeze item_count is tiny; never pretend fleet-wide heavy workers.

#### 3c. Hash-first reuse
- Look for existing freeze/admit stamp hash before rewriting.
- Hit → verify fields still match checkbox + briefing; do not silently overwrite.
- Miss → write new seal pack.
- Prompt-only tweaks later must not force bar/POI re-walk (those caches do not exist yet at Freeze).

#### 3d. Machines
- Stamp write: paper / prompts / dash dest tree only.
- **Blocked:** live engine, fold, VPS deploy, deleting factory ORB code elsewhere, paper-machine misuse as live.
- Side-copy vs live: this room is **paper / prompts only**.

---

### 4. Step-by-step (overseer path)
1. Read CURRENT_STAGE_MAP + PROMPT_DASH_HANDOFF + this handbook. Confirm ORB DEAD, doubles DEFUNCT, fleet ES+NQ 5m+15m, fork hooks only.
2. Read STRATEGY INPUT checkbox → bind entry-method set id; **strip any orb_*; refuse doubles/pairs**. If Continuation selected, stamp set as **OPEN** (catalog minus orb_*), never LOCKED.
3. Build / confirm **cost model** pack (must exist before any later PF talk). Stamp cost-applied-required flag for all downstream WORKs.
4. Build / confirm **session locks** (RTH vs overnight / calendar rules in force). Do not invent exotic filters (on/off list is parked for Stage 14).
5. Admit fleet roster: exactly ES 5m, ES 15m, NQ 5m, NQ 15m. Reject 1m. Reject missing cell.
6. One-cell identity smoke: verify one cell folder/TF + no ORB/doubles copy in planned stamps/UI strings.
7. Sequential admit for remaining cells; paint each strip with real chunk times (identity checklist chunks — no fake %).
8. Write book-level FREEZE_ADMIT seals (see §6).
9. Heartbeat under stack until all four strips 100% green.
10. Barrier: all cells admitted → unlock Stage 2 Baseline WORK. Do **not** auto-run Baseline. Do **not** invent a Stage-1 CHECK (next CHECK is Stage 3 after Baseline).

---

### 5. Progress / UI paint
- Four stacked blue strips (ES 5m, ES 15m, NQ 5m, NQ 15m); each 10 chunks (10%…100%) + timestamps under chunks.
- Heartbeat / stuck under stack.
- Paint rules: no fake ticks; Silent Zero = fail; percent paint without real admit checklist work = fail.
- No orange CHECK on Stage 1 itself; Stage 2 WORK unlocks when freeze stamps + strips complete. Later stages: orange CHECK stays locked until every strip 100% green.

---

### 6. Success stamps
- `freeze_admit/BOOK_SEAL.json` — cost model id/hash, session lock id, fleet roster, STRATEGY INPUT checkbox value, entry_set_id, entry_set_lock_state (LOCKED|OPEN), orb_dead=true, doubles_defunct=true, fork=hooks_only, paper_only=true
- `freeze_admit/COST_MODEL.json` — fields required before any PF talk downstream
- `freeze_admit/SESSION_LOCKS.json` — RTH/overnight/calendar locks in force
- `freeze_admit/ENTRY_SET.json` — Reversal 6 names OR Continuation OPEN catalog minus orb_* (explicit exclusions listed)
- `freeze_admit/PROVISIONAL_BAND_PF_NOTE.json` — skip <0.75; revival ~0.75; boost 1.0|1.2 **not locked** (disclose only)
- `freeze_admit/CELL_ADMIT/{cell}.json` — per-cell roster/TF admit + workers_used honesty
- `freeze_admit/MANIFEST.json` — hashes, workers_used, n cells=4, timestamp
- What CHECK will read later (Stage 3+): cost/session locks present; no ORB in intake; entry set matches checkbox; provisional PF note not forged as final.

---

### 7. What NOT to do
- No ORB / no doubles (do not put in UI copy or stamps except DEAD/DEFUNCT markers)
- No factory code edits from this room (do not delete ORB code elsewhere)
- **No sharding the freeze / admit seals / identity decisions**
- No inventing OPEN David locks (0.75/1.2 final, continuation shortlist, take rule, on/off list, POI list)
- No advancing without fleet 100% admit strips green
- No contaminating holdout (last ~20% reserved for take only — later stages)
- No baseline / POI / band / boost work in this stage
- No claiming Continuation LOCKED
- No live / fold / VPS language in seals

---

### 8. Heartbeat / stuck
- Heartbeat cadence: under stack, last cell admit step + ETA
- Stuck definition: no chunk advance within threshold / no stamp write / worker death (should be rare — one-core)
- Recovery: restart **Stage 1 only**; do not jump to Baseline; preserve any valid seal fields already verified

---

### 9. Cheap loops KEEP (stage-relevant subset)
- Cost model early (before any PF)
- One-cell identity smoke before fleet admit paint
- Roster defaults hunt (wrong folder / 1m leak)
- ORB/doubles copy leak hunt in planned UI strings
- Cache never re-walk *(N/A bars yet — pin the rule for later)*
- Do not invent OPEN items

---

### 10. Handoff to next
- Unlock Stage 2 Baseline alone WORK (`02_BASELINE_WORK.md`)
- Stage 3 CHECK (`03_BASELINE_CHECK.md`) comes **after** Baseline — point agents at CHECK_ASSAULT_OUTLINE.md Stage 3 bullets
- Artifacts Baseline must receive: BOOK_SEAL, COST_MODEL, SESSION_LOCKS, ENTRY_SET, PROVISIONAL_BAND_PF_NOTE, CELL_ADMIT ×4, MANIFEST
- Explicit attestation: no ORB / no doubles / fleet 4 cells / fork hooks only / band PF provisional

---

## Author checklist
- [x] All 10 template sections filled
- [x] MUST-SHARD vs MUST-NOT respected (Freeze = MUST-NOT heavy)
- [x] ORB DEAD + doubles DEFUNCT stated
- [x] Fleet 4 cells + 10% strips + heartbeat stated
- [x] Hash-first reuse stated
- [x] OPEN David items named, not invented
- [x] Locked pack for later stages listed
- [x] Handoff points at Baseline WORK + Stage 3 CHECK outline
