# Stage 2 — Baseline + first-sweep (TP/method) WORK handbook (11 Sep 2026)

Owner: DASHBOARD OVERSEER. Paper only. No factory code edits.
Source locks: CURRENT_STAGE_MAP.md, PROMPT_DASH_HANDOFF.md, WORK_HANDBOOK_TEMPLATE.md,
`/workspace/tick-handoff/PHASE1_FIRST_SWEEP_PLAN_20260911.md`,
`/workspace/tick-handoff/H2_STAGE2_LOOP_FREEZE_ASK_DAVID_20260911.md`.

**Display intent:** Baseline + first-sweep (TP/method). Stage number stays **2** (two-part WORK — not Stage-2b; do not renumber 3–21).

**Product dead (never revive):** Doubles/pairs = DEFUNCT. ORB = DEAD.
**Fleet:** ES + NQ × 5m + 15m only (no 1m). 10% progress strips per cell; orange CHECK only when all green.
**Fork:** hooks only.
**Band PF LOCKED (David grill 5 Sep):** PF < 0.75 = do not sweep at all; PF ≥ 0.75 = sweep (revival/fix try; must not worsen vs pre-sweep); BOOST/KEEP label only if PF > 1.2; under 1.2 after sweep = SKIP for confluence/table. No 1.0 keep line (unused fork-hook note only). Reason: compounding / fewer loser streaks. See `DAVID_GRILL_LOCKS_2026-09-05.md`.
**On/off filters:** binary no-line states; parked — ask David later.
**Paper champ:** `esnq_v2_champion_20260701` — **untouched** this stage. Tick invent = **PARKED**. Soft KEEP never replaces paper freeze.

**Phase1 plan (binding):** `/workspace/tick-handoff/PHASE1_FIRST_SWEEP_PLAN_20260911.md`
**LOOP + ASK DAVID (binding):** `/workspace/tick-handoff/H2_STAGE2_LOOP_FREEZE_ASK_DAVID_20260911.md` — hard pointer. Legal-next ≡ Phase1 §2.3 sparse matrix / H2 §1.4 only. Do **not** free-hypothesise or expand legal-next after LOSE.

---

## MUST-SHARD vs MUST-NOT (reminder)

| MUST-SHARD (heavy compute) | MUST-NOT (identity / decision) |
|----------------------------|--------------------------------|
| Bar load | Freezes / admit seals |
| **Bar baseline + bar Fixed-R screens** ← Part A + Part B screen | Checks (all of them) |
| **Tick honesty windows on dual-green survivors** | Soft KEEP / Promote tags · champion talk |
| POI map / Band find / sweep / Boost / Assemble / Stress / MC *(later)* | Take / KEEP / Soft KEEP / Promote decisions |
| | Hold / grade alone |
| | All-cell barrier wait (100% fleet) |
| | Speedy lamp / emit-covered report steps |
| | Champion talk-box choice |

**Hard rule:** Baseline + first-sweep bar screens **are** MUST-SHARD. Tick honesty windows on dual-green survivors **are** MUST-SHARD. Soft KEEP / Promote tagging, champion talk, and barrier wait are MUST-NOT. Wait 100% across all fleet cells before Stage 3 CHECK unlock. Do not shard the barrier wait itself or the CHECK.

---

### 0. Header
- **Stage # / name:** 2 — Baseline + first-sweep (TP/method)
- **Kind:** WORK (two-part: **Part A** baseline floors · **Part B** Phase1 first sweep)
- **Goal (one sentence):** Stamp per-cell no-TP / paper-style floors (Part A), then run sparse Fixed-R + entry clocks on honesty path, fill §4 scorecard Soft KEEPs (Part B) — Soft KEEP ≠ promote; unlock Stage 3 CHECK.
- **Unlocks next:** Stage 3 CHECK
- **Fleet cells:** ES 5m, ES 15m, NQ 5m, NQ 15m
- **STRATEGY INPUT:** Checkbox selects entry set — Reversal LOCKED (`close_back_inside`, `n_close_back_inside`, `wick_rejection_completed_bar`, `touch`, `close_confirm`, `pullback_to_level`) OR Continuation PLACEHOLDER (catalog minus `orb_*` — not operable OPEN; do not claim locked).

---

### 1. Goal
- **Done means:**
  - **Part A:** Per fleet cell: no-TP / paper-style ctl (hash-first, cost-applied) complete; n+PF floors stamped; mediocre PF (~0.9 / breakeven) accepted as yardstick — this **is** the dual_vs_noTP control book.
  - **Part B:** Sparse Fixed-R + entry clocks on honesty path per Phase1 §2.3 / H2 §1.4; §4 scorecard filled; Soft KEEP list parked (no promote); honesty lamp attestation + MENU ladder gap note on stamp.
  - Bar-cache hashes pinned; strips 100% green; book-level baseline + first-sweep manifest + one-cell smoke identity written.
- **Does NOT decide:**
  - Promote / paper replace (forbidden here without full gate — H3 will enforce UI later; handbook already FAILs promote talk)
  - Final champion take (talk-box only if multiple baseline champion **types** **or** per-cell Soft KEEP research-shelf choice — still hooks only; Soft KEEP ≠ paper replace)
  - POI / band / boost / confluence / on-off / assemble
  - Locking 0.75/1.2 or Continuation set
  - Free-hypothesise beyond legal-next (H2)

---

### 2. Stamped inputs (must exist before start)
If missing → STOP, do not invent.
- **Freeze/admit:** `freeze_admit/BOOK_SEAL.json`, `COST_MODEL.json`, `SESSION_LOCKS.json`, `ENTRY_SET.json`, `PROVISIONAL_BAND_PF_NOTE.json`, `CELL_ADMIT/{cell}.json`, `MANIFEST.json`
- **Prior WORK stamps:** Stage 1 only (no Stage 3 CHECK yet — that comes after this WORK)
- **Cache / hash pins available:** look for existing bar hashes per cell; never re-walk if pin matches code+roster
- **Entry-method set in force:** current STRATEGY INPUT checkbox value from Freeze seal (must match); strip orb_*; no doubles/pairs
- **Phase1 / H2 binding files readable:** plan + H2 LOOP card paths above

---

## Part A — Baseline floors (dual_vs_noTP control book)

Per-cell **no-TP / paper-style ctl** (champ exit mix: EOD+stop+BE+trail). Hash-first, cost-applied. Mediocre PF expected as yardstick — juice is not claimed here; Part B measures against this ctl.

### A.1 Per-cell freeze table (Phase1 §1.1) — load refuse if paste

Portfolio: **`esnq_v2_champion_20260701`**. Money column: choosing-window **`eod_pnl_R`**. Do **not** use live-config `managed_pf`.

| Cell | Stop (approx) | BE | Trail | Entry style (champ) | File TP |
|------|---------------|----|-------|---------------------|---------|
| **ES15** | ~23.1 ticks (~5.8 pts) | consec **3** × 1m profit closes → stop entry **+1 tick** | Donchian/swing **N=20** on 1m | touch-through MIT, RTH 09:30–16:00, flatten ~15:55 ET | rr ~**30** (dead — never hits) |
| **ES5**  | ~106.3 ticks (~26.6 pts) | consec **3** +1 tick | swing **20** | same family | rr ~**9.5** (rarely hits) |
| **NQ15** | ~220.7 ticks (~55.2 pts) | **off** (consec 999) | swing **20** | same family | rr ~**29** |
| **NQ5**  | ~82.7 ticks (~20.7 pts) | after **1** profitable 1m close (offset 0) | **none** | same family | rr ~**22** |

**Freeze rule:** load **that cell’s** champ recipe. **Refuse** if NQ loaded with ES BE/trail (or any cross-cell paste). When Part B changes **one** oracle (TP **or** entry clock), leave the other champ settings frozen. Never retune stop+TP+BE together in first sweep.

### A.2 Part A done means
- Four cells: no-TP ctl stamped with cost-applied n+PF; bar hashes pinned.
- Mediocre PF (~0.9 / breakeven) stamped as baseline reality — do not polish into “edge.”
- dual_vs_noTP control book ready for Part B arms.

---

## Part B — Phase1 first sweep (TP/method)

Screen **reachable TP + entry clocks** on sealed **honesty** path; score every arm on §4 scorecard; Soft KEEP ≠ promote; thin optional NQ/ES5; no ES15-only silent crown; reachable R20 ≠ widen family.

### B.1 Honesty lamps (Phase1 §2.1) — mandatory before Soft KEEP / promote talk

Every scored TP arm must run on sealed honesty:

1. **EOD-gated F2** — no ungated fantasy fill past flatten deadline
2. **Stage7 precede** — BE/trail/stop can beat TP on timestamp ties
3. **SL-first same-ts race** (`<=` stop before TP fantasy)
4. Report **fantasy ΔR** (headline vs honesty) on same trade ids
5. **Never promote** if honesty dual dies even if choosing PF looks great

Fail closed on missing honesty lamps. Fantasy-only PF is not Soft KEEP juice.

### B.2 Sparse matrix (Phase1 §2.3) — not full Cartesian

Default arms (cores-aware):

1. Per cell: no-TP ctl (Part A / champ) — already on board
2. Per cell: Fixed-R **1.5** honesty on **champ entry**
3. Per cell: Fixed-R **1.0** and **2.0** honesty on **champ entry** (ES15 full; other cells may defer 1.0/2.0 if 1.5 already dual-fails hard — thin rules)
4. Per cell: **at least one** close-wait × no-TP ctl before calling the entry shelf exhausted
5. **ES15 priority:** close-wait × Fixed-R **1.5** honesty — **import/re-lamp** known book-identity rows into §4 columns first if trade ids match; full recompute only if factory path differs. CONDITIONAL only — do not auto-promote
6. Do **not** run full FT×CW×rr×cell product

Arm IDs: `TP_FIXED_R_R10_F2`, `TP_FIXED_R_R15_F2`, `TP_FIXED_R_R20_F2`; `ENTRY_TOUCH` / pierce; `ENTRY_CLOSE_WAIT`; optional `ENTRY_1M_CLOSE` if already on factory menu.

**Reachable R20 ≠ widen family.** `TP_FIXED_R_R20_F2` is one reachable grid point — not `PRIOR_RANGE` / always-2R / ATR widen. If R20 dual-fails, park R20 — do not fish.

### B.3 Bar-first / tick-on-survivors only

- **Bar / 1m first** for fleet screen.
- **Tick** only when claiming F2 honesty PASS / fill races, and **only on dual-green survivors** (and always before Soft KEEP→promote talk).
- Prefer re-lamp known ES15 CW+FR packs into §4 if ids overlap; verify n/overlap before “re-lamp.”

### B.4 Year + holdout + cost columns (declare cut once)

**Holdout cut (declared once for this stage):** default **`entry_time ≥ 2022-02-09`**. Pack “last ~20% untouched” is resolved as: **holdout dual print** using this declared cut vs no-TP ctl (+ harvest reject PF↑/sumR↓). Do not leave both vague. If factory-equivalent OOS differs → **ASK DAVID** (H2 D2) before scoring.

Every surviving TP/method cell prints:

- Year dual table with at least **2016 + 2020 + meat years** (n≥30)
- Soft hole flag: **PF↑ / sumR↓** vs ctl (2020 Fixed-R pattern)
- Early-cut anatomy when TP fires and ctl went EOD (`n` / `ΔR`)
- Holdout dual vs no-TP using the declared cut
- Explicit: **no 2020-exclude promote**
- On dual-green survivors: adverse **+1 / +2 tick** entry and TP; **`commission_status`** column (must not stay `COMMISSION_NA` forever for promote)

### B.5 Soft KEEP ≠ promote

| Shelf | Meaning | Allowed |
|-------|---------|---------|
| **Soft KEEP** | Choosing dual yes (honesty); research shelf; may be YEAR_FRAGILE / ES_ONLY / COMMISSION_NA | Keep for later invent / transfer; **promote FAIL OK** |
| **Promote / paper** | Full gate below | **Forbidden in Stage 2** without full gate (H3 UI later — handbook FAILs promote talk now) |

Default promote gate (must all hold — do not claim promote here):

```
dual_vs_noTP
∧ holdout_dual
∧ ¬soft_hole_only_juice
∧ year_meat_ok
∧ commission ≠ NA
∧ ¬ES_ONLY_if_policy_requires_NQ
∧ honesty path PASS (not fantasy headline)
```

Soft KEEP allowed with `COMMISSION_NA` / `YEAR_FRAGILE`. Promote tag with those soft-hole-only juice paths = FAIL.

### B.6 Thin optional NQ/ES5 — no ES15-only silent crown

| Lane | When allowed | When forbidden |
|------|--------------|----------------|
| **NQ Fixed-R** | One frozen oracle diagnostic after ES screen if multi-cell policy on; expect LOSE | Default hope / promote / fishing r* after LOSE; second NQ FR → ASK DAVID (H2 D4) |
| **ES5 Fixed-R 1.5 transfer** | One transfer check after ES15; CONDITIONAL only | Fleet crown / skip year grill |
| **Widen Fixed-R / prior-range / ATR** | Rare optional only with written reason on non-ES15 after default grid | Default arms; τ/N/K fish after LOSE |

No ES15 Soft KEEP greened across fleet strips. Per-cell §4 rows required. Reachable R20 ≠ widen reopen.

### B.7 §4 scorecard columns (each arm × cell)

`pf_honest, sumR_honest, n, dual_vs_noTP, dual_vs_FT_or_CW_entry, holdout_dual, year_2020_dR, year_2016_dual, soft_hole_2020, early_cut_n_dR, adverse_slip_green, commission_status, soft_keep_or_promote, notes`

Also report (lamps / notes): exit mix `tp/sl/eod`, fantasy ΔR vs honesty, cell id, arm id, data path (bar|tick).

### B.8 LOOP + ASK DAVID (binding — H2)

**Binding file:** `/workspace/tick-handoff/H2_STAGE2_LOOP_FREEZE_ASK_DAVID_20260911.md`

- After every arm×cell: tag `PASS_DUAL` / `SOFT_KEEP` / `LOSE` / `INCONCLUSIVE` per H2 §1.1–1.2.
- After `LOSE`: legal-next **only** from H2 §1.4 ≡ Phase1 §2.3 sparse matrix — at most one next arm; two LOSEs on same family → STOP family on that cell.
- **Forbidden after LOSE:** τ/N/K grids, widen-always / ATR / prior-range fish, Soft KEEP stacks, second NQ Fixed-R without ASK DAVID, invent reopen, param continuous search / free-hypothesise.
- Two-shelf freeze (H2 §2): paper freeze always `esnq_v2_champion_20260701` until promote gate PASSES; Soft KEEP never replaces paper; optional `research_control` for exactly one child arm under H2 rules.
- **ASK DAVID** only on pre-registered stops H2 §3 D1–D7 (commission-as-promote, holdout mismatch, MENU/invent reopen, second NQ FR, no legal next, paper-champ pressure, blocked research_control).
- Until operators follow this card: **follow §7 / Part B sparse matrix only — no param fish.**

---

### 3. Full sharding handbook
#### 3a. Split
- **Unit:** Part A = (cell × time/bar-range shard); Part B = (cell × arm) steal-queue preferred when H4 runner bound — until then, same consistent shard key per run
- **Shard key:** prefer time/bar-range for baseline; (cell × arm) for Fixed-R / clock screens
- **Smoke:** YES — one cell (prefer NQ 5m or ES 5m — pick one) full Part A path before fleet fan-out; Part B ES15 juice first when fanning Fixed-R
- Do **not** include POI/boost arms — Stage 2 only (Part A floors + Part B sparse first-sweep)

#### 3b. Cores
- Default `n_workers=1`; for fleet heavy pass **opt-in high-n**
- **Target machines:** EPYC (up to 64) + Gaming when room rules allow — **max cores guidance for this MUST-SHARD stage**
- Honest stamp: `workers_used = min(requested_workers, item_count)` — never pretend 64 on a tiny smoke job
- Local steal OK on baseline / bar-screen executor if present on side copy; identity hash must match one-core smoke

#### 3c. Hash-first reuse
1. Lookup existing bar / baseline / Fixed-R result hash for (cell, arm_id, entry_set_id, cost_model_id, session_lock_id, code_pin, honesty_path)
2. Hit → paint from cache, **do not re-walk bars**
3. Miss → run shard; write hash + artifact path
4. Prompt-only tweaks never invalidate bar cache
5. Identity: one-core hash == multi-core hash when claiming equivalence

#### 3d. Machines
- Heavy shards: EPYC side copy / approved dest tree; Gaming if current room allows
- Do not assume two-machine split if blocked
- **This room:** paper prompts + dash only — no live engine, no fold, no VPS, no deleting factory ORB code elsewhere

---

### 4. Step-by-step (overseer path)

**Part A — floors**
1. Verify Stage 1 freeze/admit stamps all present; STOP if cost model missing or ORB/doubles attestation absent.
2. Read STRATEGY INPUT checkbox vs Freeze ENTRY_SET — must match; **strip any orb_*; refuse doubles/pairs arms**.
3. Load **per-cell freeze table** (§A.1); **refuse** cross-cell BE/trail paste.
4. One-cell smoke: baseline alone (no-TP ctl) on one cell; verify n, PF (cost-applied), workers_used honesty, hash pin.
5. Identity check: smoke `n_workers=1` hash == `n_workers=k` hash on same tiny set.
6. Fan out fleet cells; per cell load bar cache by hash; enqueue **no-TP ctl only** for Part A floors.
7. Apply early n+PF floors for sanity; **cost model before any PF talk**.
8. Expect mediocre PF (~0.9 / breakeven) — stamp as dual_vs_noTP yardstick; do not polish into “edge.”

**Part B — first sweep (§7 run order after Part A)**
9. Wire / confirm §4 scorecard columns + Soft KEEP / promote tags on Stage-2 board; **declare holdout cut once** (`entry_time ≥ 2022-02-09` unless ASK DAVID D2).
10. ES15: Fixed-R 1.0/1.5/2.0 honesty on champ entry (**bar screen** → **tick honesty seal only on dual-green survivors**).
11. ES15: entry CW vs FT shelves (with and without Fixed-R 1.5 — do not require compound). Prefer **re-lamp** known CW+FR honesty / holdout / adverse packs into §4 if ids overlap (verify n/overlap).
12. Year + holdout + adverse on ES15 survivors; fill honesty lamps §B.1.
13. Other cells: **one CW×no-TP** each; Fixed-R 1.5 transfer/diagnostic thin; NQ Fixed-R once if multi-cell policy on — expect fragile/LOSE; NQ5 at most one FR shot.
14. After each arm×cell: apply H2 LOOP tags; on LOSE use legal-next only (H2 §1.4 ≡ Phase1 §2.3); ASK DAVID on D1–D7 only.
15. Stamp Soft KEEPs; **no** promote; leave MENU ladder parked; write **MENU gap** on the stamp note (ladder / partials unmeasured).
16. **Compounding champion lens note:** if multiple baseline champion *types* **or** per-cell Soft KEEP research-shelf choice appears, flag for Stage 3 talk-box — pick for **compounding / high-% bank risk fit**, not vanity PF. Soft KEEP ≠ paper replace. If only one type / no Soft KEEP choice, no forced talk.
17. Paint each cell strip 10%…100% with real chunk times; heartbeat under stack.
18. Write per-cell baseline + first-sweep stamps + book-level manifest + smoke identity + scorecard fill path + honesty attestation + Soft KEEP list (no promote).
19. Barrier: wait all four cells 100% green → unlock Stage 3 CHECK (do not auto-pass CHECK).

---

### 5. Progress / UI paint
- Four stacked blue strips; each 10 chunks (10%…100%) + timestamps
- Heartbeat every N seconds; stuck if no chunk advance within threshold or worker death
- Silent Zero / fake tick / percent paint without jobs → fail this section
- Orange Stage 3 CHECK stays locked until every strip 100% green
- Soft KEEP = amber research shelf; Promote = emerald (display only until H3 — **do not** tag Promote without full gate)

---

### 6. Success stamps
- `baseline/CELL/{cell}.json` — n trades, PF (cost-applied), session lock id, entry_set_id, bar_hash, workers_used, mediocre_pf_note, per-cell freeze attestation
- `baseline/MANIFEST.json` — fleet cells, entry_set_id, cost_model_id, workers_used honesty, hashes, no_ORB / no_doubles attestation
- `baseline/SMOKE_IDENTITY.json` — one-core vs multi-core hash
- `baseline/CHAMPION_TYPES_NOTE.json` — optional; list types if multiple; compounding-lens flag for CHECK talk-box (no forced take)
- `first_sweep/SCORECARD/{cell}.json` — §4 columns filled per arm; Soft KEEP list; **no promote**
- `first_sweep/HONESTY_LAMP_ATTESTATION.json` — EOD-gated F2 / Stage7 precede / SL-first / fantasy ΔR reported (or N/A with reason) per Soft KEEP row
- `first_sweep/HOLDOUT_CUT.json` — declared cut (`entry_time ≥ 2022-02-09` or David-approved equivalent)
- `first_sweep/MENU_LADDER_GAP_NOTE.json` — ladder / partials unmeasured (Phase1 §4 stamp note)
- `first_sweep/SOFT_KEEP_LIST.json` — Soft KEEP arm×cell ids only; Soft KEEP ≠ promote attestation
- `first_sweep/MANIFEST.json` — sparse matrix coverage, bar-first/tick-survivor paths, workers_used honesty, Phase1 plan + H2 card pointers
- Strip paint 100% × 4 cells
- What CHECK will read: cost-applied PF tables, dual_vs_noTP ctl, §4 scorecard fill path, honesty lamp attestation, MENU ladder gap note, Soft KEEP list (no promote), smoke identity, entry set vs checkbox, mediocre-PF expectation (Part A), workers_used honesty, per-cell freeze attestation

---

### 7. What NOT to do
- No ORB / no doubles
- No factory code edits from this room
- No sharding Soft KEEP/Promote tags / CHECK / barrier wait / champion talk
- No inventing OPEN David locks (0.75/1.2, continuation set, take rule, on/off list, POI list)
- No advancing without fleet 100%
- No contaminating holdout — use **declared cut** for holdout dual print (not vague “last ~20%” alone)
- No POI / band / boost / confluence in this stage
- No talking Part A baseline PF up as proven edge
- No Soft KEEP painted as promote / paper green language
- No promote tag with COMMISSION_NA or YEAR_FRAGILE soft-hole-only juice
- No fantasy-only PF Soft KEEP (honesty lamps missing)
- No ES15 Soft KEEP greened across fleet strips / silent crown
- No per-cell freeze paste (NQ with ES BE/trail)
- No full Cartesian FT×CW×rr×cell; no param fish / widen-default / invent reopen after LOSE
- No free-hypothesise beyond H2 legal-next ≡ Phase1 §2.3
- No touching paper champ `esnq_v2_champion_20260701`; invent stays PARKED
- No re-walking bars because a prompt word changed
- No unlocking CHECK until all strips green
- No inflated workers_used on tiny smoke

---

### 8. Heartbeat / stuck
- Heartbeat: under stack, show last shard progress + ETA
- Stuck: no chunk advance for agreed window OR worker pool dead OR identity mismatch OR freeze paste refused
- Recovery: restart **Stage 2 only**; preserve bar caches + Freeze seals; do not reopen Freeze unless seal is wrong (then loop Stage 1)

---

### 9. Cheap loops KEEP (stage-relevant subset)
- One-cell smoke before fleet
- Cost model before PF
- n+PF floors early (Part A yardstick)
- Hash-first / cache never re-walk
- RTH vs overnight sanity on sample
- ES+NQ overlap awareness (flag, do not “fix” by mixing folders)
- Multiplicity control if entry-set expansion smells
- Compounding lens note for champion types / Soft KEEP shelf choice (not vanity PF; Soft KEEP ≠ paper)
- Holdout cut declared once + dual print
- Honesty lamps before Soft KEEP talk
- Sparse matrix only; H2 LOOP after LOSE
- Bar-first / tick-on-survivors

---

### 10. Handoff to CHECK
- Feed Stage 3 bullets in CHECK_ASSAULT_OUTLINE.md / `03_BASELINE_CHECK.md`
- Give agents: baseline CELL stamps ×4, MANIFEST, SMOKE_IDENTITY, Freeze COST_MODEL + ENTRY_SET + SESSION_LOCKS, CHAMPION_TYPES_NOTE if any, first_sweep SCORECARD + HONESTY_LAMP_ATTESTATION + HOLDOUT_CUT + MENU_LADDER_GAP_NOTE + SOFT_KEEP_LIST + MANIFEST, explicit “no ORB/no doubles” + “mediocre PF expected (Part A)” + “Soft KEEP ≠ promote” attestation, workers_used honesty proof, Phase1 plan + H2 card paths

---

## Author checklist
- [x] All 10 template sections filled
- [x] Two-part Stage 2 (Part A floors + Part B first sweep) — no Stage-2b / no renumber
- [x] MUST-SHARD vs MUST-NOT respected (bar baseline + bar Fixed-R + tick honesty survivors = MUST-SHARD; Soft KEEP/Promote/champion/barrier = MUST-NOT)
- [x] Per-cell freeze table + load refuse if paste
- [x] Honesty lamps §2.1 + sparse matrix §2.3 + bar-first/tick-on-survivors
- [x] Holdout cut declared (`≥ 2022-02-09`) as holdout dual print
- [x] Soft KEEP ≠ promote; promote talk FAIL without full gate
- [x] Thin NQ/ES5; no ES15-only silent crown; R20 ≠ widen
- [x] H2 LOOP + ASK DAVID hard pointer (legal-next ≡ Phase1 §2.3 / H2 §1.4)
- [x] ORB DEAD + doubles DEFUNCT; fleet 4 cells; hash-first; workers_used honesty
- [x] Paper champ untouched; invent PARKED
- [x] Success stamps include scorecard + honesty attestation + MENU gap + Soft KEEP list (no promote)
- [x] Phase1 plan path bound
- [x] Handoff points at Stage 3 CHECK assault
