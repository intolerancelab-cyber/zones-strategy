# Stage 3 — CHECK assault pack (after Baseline + first-sweep) (11 Sep 2026)

Owner: DASHBOARD OVERSEER. Paper only. No factory code edits.
Source locks: CHECK_ASSAULT_OUTLINE.md, CURRENT_STAGE_MAP.md, PROMPT_DASH_HANDOFF.md,
`/workspace/tick-handoff/PHASE1_FIRST_SWEEP_PLAN_20260911.md`,
`/workspace/tick-handoff/H2_STAGE2_LOOP_FREEZE_ASK_DAVID_20260911.md`.

**Rule:** Multi-agent assault with the four shared roles + Stage 3 bullets. Fail → loop to **Stage 2 Baseline + first-sweep only**. Orange CHECK unlocks only when all fleet strips (ES+NQ × 5m+15m) are 100% green.

**Product dead (never revive):** Doubles/pairs = DEFUNCT. ORB = DEAD.
**Fleet:** ES + NQ × 5m + 15m only (no 1m).
**Fork:** hooks only.
**Band PF LOCKED (David grill 5 Sep):** PF < 0.75 = do not sweep at all; PF ≥ 0.75 = sweep (revival/fix try; must not worsen vs pre-sweep); BOOST/KEEP label only if PF > 1.2; under 1.2 after sweep = SKIP for confluence/table. No 1.0 keep line (unused fork-hook note only). Reason: compounding / fewer loser streaks. See `DAVID_GRILL_LOCKS_2026-09-05.md`.
**Continuation:** PLACEHOLDER (catalog minus orb_* — not operable OPEN; wait David before lock).
**Multi-agent:** every check (this one included).
**Paper champ:** `esnq_v2_champion_20260701` untouched. Invent **PARKED**. Soft KEEP ≠ promote / ≠ paper replace.
**LOOP binding:** `/workspace/tick-handoff/H2_STAGE2_LOOP_FREEZE_ASK_DAVID_20260911.md` — legal-next ≡ Phase1 §2.3 / H2 §1.4 only.

---

## Prior WORK context
Stage 2 ran **two-part** WORK:
- **Part A — Baseline floors:** per-cell no-TP / paper-style ctl (hash-first, cost-applied). Expect **mediocre PF (~0.9 / breakeven)** — that is the dual_vs_noTP yardstick; juice is not claimed as edge.
- **Part B — Phase1 first sweep:** sparse Fixed-R + entry clocks on honesty path; §4 scorecard filled; Soft KEEP shelf parked; **no promote**.

Champion talk only if multiple baseline champion **types** appear **or** per-cell Soft KEEP research-shelf choice — still hooks only; Soft KEEP ≠ paper replace.

**Agents must know:**
- Cost/session locks from Freeze (Stage 1) stamped; no ORB in intake.
- Entry-method set from STRATEGY INPUT checkbox (Reversal LOCKED 6; Continuation PLACEHOLDER minus orb_* (not operable OPEN)).
- Part A = floors only; Part B = Phase1 sparse matrix — no POI/boost yet.
- Soft KEEP ≠ promote; honesty lamps mandatory; holdout cut declared once (`entry_time ≥ 2022-02-09` default).
- H2 LOOP card binds LOSE→legal-next; free-fish / param fish / invent reopen = FAIL.

---

## Stamped inputs agents must receive
If any missing → FAIL (mistake hunter); do not invent.
- Stage 1: `freeze_admit/BOOK_SEAL.json`, `COST_MODEL.json`, `SESSION_LOCKS.json`, `ENTRY_SET.json`, `PROVISIONAL_BAND_PF_NOTE.json`, `MANIFEST.json`
- Stage 2 Part A: `baseline/CELL/{ES_5m,ES_15m,NQ_5m,NQ_15m}.json`, `baseline/MANIFEST.json`, `baseline/SMOKE_IDENTITY.json`
- Stage 2 Part B: `first_sweep/SCORECARD/{cell}.json`, `first_sweep/HONESTY_LAMP_ATTESTATION.json`, `first_sweep/HOLDOUT_CUT.json`, `first_sweep/MENU_LADDER_GAP_NOTE.json`, `first_sweep/SOFT_KEEP_LIST.json`, `first_sweep/MANIFEST.json`
- Optional: `baseline/CHAMPION_TYPES_NOTE.json`
- Strip paint evidence: all four cells 100% green before this CHECK was clickable

---

## Shared multi-agent pack (EVERY check)

Spawn **four agents in parallel**. Each gets: (a) stamped inputs above, (b) fleet cell results (ES 5m, ES 15m, NQ 5m, NQ 15m), (c) this stage's cheap-kill list, (d) the four questions.

### Role 1 — Mistake hunter
**Question:** Was a mistake made?
- Hunt: Silent Zero, fake ticks, percent paint without work, wrong folder/roster, smell gaps, leftover polish, roster defaults, ORB/doubles copy leaking into UI or stamps.
- Hunt: skipped stamp, wrong entry-method set vs STRATEGY INPUT checkbox, 1m accidentally in fleet, re-walk of cached bars/POIs, identity stage that was sharded when it must not be.
- **Stage 3 add:** baseline treated as if POI/boost already ran; Freeze seals skipped; CHECK clicked before all strips green; Soft KEEP/Promote decisions sharded; per-cell freeze paste (NQ loaded with ES BE/trail); empty §4 scorecard when first-sweep claimed done; promote talk without full gate.
- Output: PASS / FAIL with concrete file/stamp/path evidence. No vibes.

### Role 2 — Number auditor
**Question:** Are these numbers legit?
- Reconcile counts, PF, n trades, workers_used honesty (`min(requested, item_count)`), hash pins vs one-core where claimed.
- Band PF LOCKED (David grill 5 Sep): PF < 0.75 = do not sweep at all; PF ≥ 0.75 = sweep (revival/fix try; must not worsen vs pre-sweep); BOOST/KEEP label only if PF > 1.2; under 1.2 after sweep = SKIP for confluence/table. No 1.0 keep line. *Baseline itself is pre-band; confirm BAND_PF_LOCKS disclosed as LOCKED (not forged alternate).*
- Cost model present before any PF talk. Holdout = **declared cut** dual print (`entry_time ≥ 2022-02-09` default) — not vague untouched-only claim.
- **Stage 3 add (Part A):** mediocre PF (~0.9 / breakeven) is expected — do not FAIL solely for “low” baseline PF if cost-applied and counts reconcile; FAIL if PF talked without cost or workers_used inflated.
- **Stage 3 add (Part B):** every Soft KEEP row must show dual_vs_noTP / holdout / year / commission lamps (or explicit COMMISSION_NA / YEAR_FRAGILE); fantasy-only PF (honesty lamps missing) = FAIL; reachable Fixed-R grid absent when claiming first-sweep done = FAIL.
- Output: PASS / FAIL with tables: claimed vs recomputed, per cell.

### Role 3 — Real-life trader lens
**Question:** Would it work in real life?
- Slippage/thin open, RTH vs overnight, ES+NQ overlap correlation, session/calendar, look-ahead on POIs (N/A yet — flag if POI leaked in), capacity if size were live, never-tag failure mode.
- Compounding / high-% bank risk: does any baseline champion **type** or Soft KEEP research-shelf choice fit staking, not generic PF alone? Soft KEEP ≠ paper replace.
- **Stage 3 failure modes:**
  - Session filter wrong (RTH vs overnight mix)
  - Tick/ATR units wrong → baseline silent-zero or absurd trade count
  - Champion / Soft KEEP picked for vanity PF instead of compounding fit
  - Soft KEEP washed into “almost paper” / Monday-ready language without promote gate
  - ES15 Soft KEEP read as fleet crown
- Output: PASS / WARN / FAIL with “what breaks on Monday morning” list.

### Role 4 — Claim refuter
**Question:** What have we done wrong?
- Attack every stamp claim: “KEEP”, “Soft KEEP”, “Promote”, “champion”, “holy grail”, “boost unlock”, “skip zone”, “confluence allowlist”, “baseline edge,” “first-sweep done.”
- Demand disconfirming evidence; null shuffle / multiplicity where relevant.
- **Stage 3 add:** refute any claim that Part A baseline alone already proves edge; refute Soft KEEP as promote / paper green; refute Continuation LOCKED; refute ORB/doubles revival; refute POI/band work done here; refute widen-family reopen disguised as R20; refute free-hypothesise beyond H2 legal-next; refute invent reopen.
- Output: REFUTED claims list + what must be re-run in **Stage 2 only** (or Stage 1 if Freeze identity wrong).

### Shared verdict protocol
1. Any FAIL from roles 1–2 → CHECK fail → loop same WORK section (Stage 2; Stage 1 only if freeze identity broken); do not advance.
2. WARN from role 3 → talk box + champion checkpoint/bank only if a real choice exists (e.g. multiple baseline champion types **or** per-cell Soft KEEP research-shelf choice); Soft KEEP ≠ paper replace; else stamp WARN and proceed only if David/overseer accepts.
3. Role 4 REFUTED items that are fixable → same-section loop; if OPEN with David → park, do not invent (ASK DAVID per H2 D1–D7).
4. All four PASS (or WARN accepted) → stamp CHECK green; unlock Stage 4 POI map WORK.

---

## Shared cheap kills (all stages)
- Silent Zero / fake tick / percent paint
- Wrong folder or wrong roster defaults
- ORB or doubles copy in UI/stamps
- Fleet missing a cell or including 1m
- CHECK clicked before all strips 100% green
- Hash/reuse skipped when prior stamp exists

---

## Stage 3 — cheap kills specific

**Part A (mediocre-baseline-as-edge — keep):**
- Baseline PF talked up as “edge” already
- Cost model missing before PF
- Wrong entry set vs checkbox
- One-cell smoke skipped before fleet
- workers_used inflated on tiny job count

**Part B (Phase1 first-sweep — ADD):**
- Soft KEEP painted as promote / paper green language
- Empty §4 scorecard / missing dual_vs_noTP / holdout / year / commission lamps on any Soft KEEP row
- Fantasy-only PF (honesty lamps missing)
- ES15 Soft KEEP greened across fleet strips / silent crown
- Reachable Fixed-R grid absent when claiming first-sweep done
- Per-cell freeze paste (NQ loaded with ES BE/trail)
- Promote tag with COMMISSION_NA or YEAR_FRAGILE soft-hole-only juice
- Param fish / widen-default / invent reopen / free-hypothesise beyond H2 §1.4 ≡ Phase1 §2.3
- Holdout cut undeclared or dual print skipped while Soft KEEP claimed
- MENU ladder gap note missing on first-sweep closeout stamp

---

## Stage 3 — real-life failure modes
- Session filter wrong (RTH vs overnight mix)
- Tick/ATR units wrong → baseline silent-zero or absurd trade count
- Champion picked for vanity PF instead of compounding fit
- Soft KEEP washed to “ready for paper” without promote gate + commission
- ES15 research juice silently crowned across NQ/5m strips

---

## Talk-before-check (only if real choice)
- If `CHAMPION_TYPES_NOTE` lists **multiple** baseline champion types **or** Soft KEEP research-shelf choice across cells → **talk box + champion checkpoint**: compounding / high-% bank risk lens (not max PF vanity). **Bank unused champion** via checkpoint hook for later resume. Fork = hooks only (no fork UX). Soft KEEP ≠ paper replace — paper freeze stays `esnq_v2_champion_20260701` until promote gate PASSES.
- Stamp `checks/stage_03_baseline/CHECKPOINT_HOOKS.json` when dual/multi types or Soft KEEP shelf choice talked (fork stamp + banked unused champion id/path).
- If single type or none → no forced talk; proceed to verdict.
- Do **not** invent take locks, POI list, 0.75/1.2 final, or Continuation shortlist (Continuation = PLACEHOLDER).
- Do **not** treat Soft KEEP as Promote.

---

## Success stamp (CHECK green)
- `checks/stage_03_baseline/VERDICT.json` — roles 1–4 PASS/WARN/FAIL, evidence paths, talk-box / checkpoint outcome if any
- `checks/stage_03_baseline/GREEN.stamp` — unlocks Stage 4 POI map WORK
- `checks/stage_03_baseline/CHECKPOINT_HOOKS.json` — if multi champion types / Soft KEEP shelf choice: fork stamp + banked unused champion (hooks only)
- Attestations: no ORB, no doubles, fleet 4 cells, cost-before-PF, mediocre-PF-expected (Part A) acknowledged, Soft KEEP ≠ promote, honesty lamps present on Soft KEEP rows, holdout cut declared, MENU ladder gap noted, walk-forward/holdout dual held while picking (Q5 spirit), Continuation still PLACEHOLDER, paper champ untouched, invent PARKED

---

## Fail loop
- FAIL → return to **Stage 2** (`02_BASELINE_WORK.md`) only; preserve Freeze seals unless Role 1 proves Freeze identity break → then Stage 1 only.
- Do not skip to POI map. Do not start band/boost. Do not expand legal-next beyond H2 §1.4 / Phase1 §2.3.
